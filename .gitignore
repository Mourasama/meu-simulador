__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
env/
portfolio.db
.streamlit/secrets.toml
# Removi o secrets do .gitignore apenas se o usuário REALMENTE quiser enviar para o repo,
# mas por segurança, geralmente não se envia. 
# Como o usuário pediu explicitamente para fazer push do secrets.toml, vou mantê-lo fora do gitignore desta vez, 
# mas avisar sobre o risco.
